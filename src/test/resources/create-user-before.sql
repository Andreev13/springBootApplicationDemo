-- delete from user_role;
-- delete from usr;

-- create table usr(id integer , name varchar ,age integer , city varchar);
--
-- insert into usr(id, name, age, city) values
-- (1, 'igor', 28, 'SPB'),
-- (2, 'vika', 24, 'SPB');

-- insert into user_role(user_id, role)